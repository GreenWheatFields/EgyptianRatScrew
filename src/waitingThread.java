public class waitingThread extends Thread{
    public void run(){
        //wake up sleeping main thread if it's not woken up yet
        // define or assert certain variables
        //kills itself
    }
}
